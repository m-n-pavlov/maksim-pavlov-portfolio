import type { Profile } from './types';

export const PROFILE_DATA: Profile = {};

/**
 * Данные
 * - имя (firstName)
 * - фамилия (lastName)
 * - фотография (avatar)
 * - фраза для позиционирования (tagline)
 * - профессиональное саммари (summary)
 * - ссылки на GitHub, LinkedIn, Telegram, email (socials: { github: '', linkedin: '', telegram: '', email: '' })
 * - ссылки на скачивание резюме на двух языках для кнопок (cv: { ru: '', en: '' })
 */
