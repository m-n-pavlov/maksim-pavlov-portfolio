/**
 * Public API модуля Theme
 * - отдаем только необходимые инструменты для работы с темой,
 *   скрывая внутреннюю логику контекста
 */
export { ThemeProvider } from './ThemeProvider';
export { useTheme } from './useTheme';
export { Theme } from './Theme';
